# 已知缺口收尾

用户已授权继续修复已知问题。本轮接续 `a76ed7c`，补齐新增多币种文案的国际化和净资产估值日期边界。使用现有工作目录和内置子 agent；不使用 worktree、Orca 或 Grok。

## 实现约定

- 新增系统文案统一进入现有 `src/i18n.ts`。保留用户分类名、备注、币种代码和金额精度，语言切换不能改变待提交草稿或冲突处理行为。
- 净资产估值日期必须是合法日历日期，且不晚于家庭时区的今天。API 保留 `asOf` 必填合同，在读取估值及调用汇率服务之前拒绝非法或未来日期。
- 前端缺省日期补家庭今天。显式空值、非法或未来日期回退到家庭今天，使用 replace 修复 URL 并保留原因提示；币种和日期同时非法时也应稳定恢复。
- 不修改投资观察日期、普通交易日期或生产数据。原有未提交的 `ui-implementation-plan.md` 和无关证据文件保持原样。
- 独立浏览器验收新增发现：净资产图纵轴金额超出 SVG 左边界约 29px，`CN¥16,000.00` 首位与币种被裁切。将同类净资产、原币收支图的轴标签修复纳入本轮，保持账务金额不变，并补大金额、负数、JPY、中英文和窄屏验证。

## Git 检查点与分工

1. 提交本轮方案。独立验收 agent 先脑暴并记录反例。
2. 日期 agent 与国际化 agent 并行工作。服务端日期边界先完成并提交 `f95e90e`；国际化迁移、插值保真及相关回归随后提交 `c5495d1`。
3. 日期 agent 接手国际化 agent 冻结后的报表前端。完成日期校验、URL 恢复和测试后提交独立检查点。
4. 独立验收 agent 对冻结代码执行完整测试、typecheck、build 和隔离数据库浏览器验收。`785fc2e` 已通过 95 文件、797 测试、typecheck 和日期浏览器检查；新增的图轴裁切问题由国际化 agent 按 TDD 修复，单独提交检查点后重新执行完整门禁与浏览器复验。
5. 提交验收结果，推送 `feat/multi-currency`，备份 ser8 正式数据库并保留回滚镜像，部署后核验健康、数据数量和只读 API。

## 验收重点

独立用例记录在 `../known-gaps-acceptance.md`。必须覆盖双非法参数同时恢复、历史日期保留、家庭时区跨日、bootstrap 未完成时不错误请求、回退提示持续、浏览器后退，以及用户文本包含占位符时仍原样显示。

此前 757 条测试通过是历史证据，不代表本轮通过。原生屏幕阅读器和原生浏览器 200% 缩放仍需单独的实际设备证据，不将模拟结果冒充原生验收。

## 状态

实现及独立验收完成，待部署。方案检查点 `05e4255`、服务端日期检查点 `f95e90e`、国际化检查点 `c5495d1`、日期前端检查点 `785fc2e`、图轴检查点 `9b8fbe2` 已提交。最终独立 96 文件、804 测试、typecheck 和 build 通过；日期 17 项浏览器检查及图轴 8 组检查通过，真实 409、家庭跨日和慢启动验证通过。ser8 在线备份已创建为 `/data/backups/pre-known-gaps-785fc2e-20260905.db`，完整性检查通过；备份时正式库为 10 个账户、202 笔交易。
