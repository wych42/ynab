# 体验发布的已知边界

2026-09-05 更新：已知国际化和估值日期缺口，以及本轮发现的图轴金额裁切，均在产品版本 `9b8fbe2` 完成独立验收。全量 96 个文件、804 条测试、typecheck 和 build 通过。详见 `known-gaps-acceptance.md`；这不代表此前 147 条验收用例均已完整通过。

## 原生辅助技术仍需人工检查

键盘操作、焦点恢复、角色、实时播报属性和文字内容均有浏览器证据。原生屏幕阅读器的实际朗读未执行；浏览器原生 200% 缩放也未成功触发，仅有 CSS 200% 放大的布局证据。继续保留这些验证边界，不将 DOM 契约当作完整听读结果。

## 估值日期边界已关闭

`MC-UI-NW-009` 已通过本轮复验。净资产 API 在估值和 FX 请求前拒绝非法、空值或未来日期；页面恢复到家庭时区今天，使用 replace 修复 URL 并保留中英文提示。实际浏览器验证了双非法参数、历史日期、后退前进、慢 bootstrap 和家庭跨日场景。

## 新文案管理已关闭

`MC-UI-I18N-008` 已通过本轮复验。新增多币种文案进入现有 i18n，包含冲突窗口、空状态、图表表头和可访问名称。翻译插值也已修复，用户文本中的 `$&` 和 `{fallback}` 保持原样。真实 409 验证了语言、草稿、取消、焦点及明确重试后的数据读回。

## 图轴金额裁切已关闭

浏览器检查发现净资产图的 `CN¥16,000.00` 越出 SVG 左边界，首位被裁切。净资产和原币收支两图已按完整金额预留轴宽，窄屏采用可聚焦的局部横滚。中英文、桌面与 390px 窄屏共 8 组实际几何检查通过；大额负数和 JPY 极值由 helper 测试及源码复核覆盖。

## 后续处理依据

原始逐项结果保留在 `ui-final-acceptance.md`，本轮关闭项以 `known-gaps-acceptance.md` 为准。原生辅助技术仍按上文保留验证边界。未提交的旧实施方案和旧验收报告保持原样。
