# ser8 体验部署结果

## 已知缺口修复部署（2026-09-05）

国际化、估值日期和图轴金额裁切修复已正式部署。产品代码为 `9b8fbe22ce962c424e8e52f9f530d858d5485b3a`，构建来源为仅附加验收记录的 `97852cca964d383a199672b7a90f5d20b3cb82f6`。入口和原密码不变。

- 新镜像：`sha256:3974695cb9f9a5356914419e4196500886b8ae5b769355b6aae6432be9ecba49`；容器 healthy。
- 从本机经 Tailscale 请求首页返回 200，正式页面引用新资源 `/assets/index-DSJjrrsy.js`。
- 现有认证保持启用；容器内使用临时令牌做接口检查，未输出密码或令牌。
- 空日期及 2026-02-30 返回 `400 invalid_date`，2099-01-01 返回 `400 future_valuation_date`。家庭今天 2026-09-05 的净资产报告完整，缺失汇率为 0。
- 原币收支保留五种启用币种：CNY、USD、SGD、JPY 有活动，EUR 无活动；投资默认日期和 12 个月范围正常。
- 部署前后均为 10 个账户、202 笔交易；正式库 `quick_check` 为 `ok`。没有写入测试交易。
- 本轮备份为 `/data/backups/pre-known-gaps-785fc2e-20260905.db`，备份检查为 `ok`。回滚镜像为 `xiaowen-ynab:pre-known-gaps-9b8fbe2`，ID 为 `sha256:7f49a586b9013fc0208147d7ef3d36aba8d1eb4190ccf39b8b82ee26c8291f28`。
- 独立验收为 96 文件、804 测试、typecheck、build 通过，本轮 18 项检查通过。原生读屏和原生缩放仍保留历史验证边界；没有将隔离环境的浏览器测试声称为正式页面已登录端到端测试。

完整签字与各层证据见 `known-gaps-acceptance.md`。独立验收临时服务已停止，临时数据库和原始证据保留。下面保留首次体验部署记录，供追溯。

## 首次体验部署记录

2026-09-05 已完成部署。入口为 http://100.99.116.33:3001/ ，继续使用原登录密码。

## 运行版本与验证

- 产品代码：`d4bb3d1107ffb935a881d09da8a293f152dc5559`。
- 镜像构建来源：`a26ec0f3081a5ae865619bd8b55e91e82b77cdde`，该提交只在已测产品代码之上增加验收文档。
- 镜像：`sha256:7f49a586b9013fc0208147d7ef3d36aba8d1eb4190ccf39b8b82ee26c8291f28`。
- 远端目录：`/home/chichi/apps/xiaowen-ynab`，分支 `feat/multi-currency`，快进后工作区干净。
- 容器 `xiaowen-ynab` 为 healthy；从本机经 Tailscale 请求首页返回 200。
- 启动日志确认应用迁移 12：`currency-ledger-revision`，随后正常监听 3001。

## 数据保留与回滚点

部署前使用 SQLite 在线备份保存 `/data/backups/pre-ui-d4bb3d1-20260905.db`，备份的 `quick_check` 为 `ok`。该路径位于 `xiaowen-ynab_budget-data` 数据卷内，未覆盖既有备份。

部署前后均为 10 个账户、202 笔交易。部署后数据库 `quick_check` 仍为 `ok`。未向正式库写入测试交易，也没有修改 `.env` 的密码、密钥或端口配置。

旧镜像保留为 `xiaowen-ynab:pre-ui-d4bb3d1`，ID 为 `sha256:0dbb07e4e8607d4a8e3a8d1f5d00220b680ac54d6999e901b9774837994cc7bd`。若后续回滚涉及数据库恢复，须先保存发布后的新增数据，不能直接覆盖当前数据库。

## 正式接口检查

检查在容器内使用现有认证模块签发临时令牌，令牌和密码没有输出。没有关闭认证。

- bootstrap 返回默认启用的 CNY、USD、SGD、EUR、JPY。
- 收支总览包含全部启用币种，CNY、USD、SGD、JPY 为有活动，EUR 为无活动。
- 逐一请求五种币种的收支详情，返回币种均与请求一致。
- 无参数投资入口返回成功，包含一个投资账户；默认日期为 2026-09-05，观察期为 12 个月。
- 汇率状态返回实际缓存覆盖及日期，CNY 显示无需换算。
- CNY 统一净资产报告完整，缺失汇率数量为 0，总额存在。

## 验收边界

本地独立门禁为 91 个文件、757 条测试通过，类型检查与构建通过。147 项验收中 125 通过、19 部分验证、1 不适用、2 项 P2 未通过。所有已确认的 P0 产品缺陷已复验关闭；原生读屏仍为 P0 部分验证。

完整结果见 `ui-final-acceptance.md`，待补项见 `ui-release-followups.md`。实际浏览器工作流验收使用隔离数据库；正式部署验证使用首页可达性、认证接口与数据库只读检查，没有冒充正式页面的已登录端到端验收。
