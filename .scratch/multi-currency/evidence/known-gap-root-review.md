# 协调者复核

产品检查点为 `9b8fbe22ce962c424e8e52f9f530d858d5485b3a`。本轮实现和测试执行均交给内置子 agent，协调者负责源码审查、证据核对、提交和部署。

## 源码与证据

- 检查 `server/reports.mjs`：日期经过严格解析后，在 `loadLedger` 和 FX 调用之前与家庭时区今天比较。默认时钟复用 `todayYmd`，测试通过注入时钟固定日界。
- 检查 `src/netWorthRoute.ts` 与报表页：币种、日期一起归一化，净资产恢复逻辑只有一个 URL 修复 effect；bootstrap 未完成时请求 key 为 null。回退原因绑定修复后地址，语言切换使用当前翻译函数重新渲染。
- 检查 `src/i18n.ts` 与共享写入客户端：翻译迁移保留原草稿和 revision 流程；插值改为对模板单次替换，不再重新解析用户提供的文本。
- 查看真实浏览器截图时发现净资产纵轴金额首位被裁切；独立 agent 随后确认文本越过 SVG 左边界约 29px。新增图轴检查点保留完整格式化金额，使用足够的轴宽和可聚焦的局部横滚容器，不改变数据值。
- 直接核对独立最终测试 JSON：96 个测试文件、804 条测试通过，失败和 pending 均为 0。浏览器及最终发布判断以 `../known-gaps-acceptance.md` 为准。

## 变更保留

原有 `ui-implementation-plan.md` 的未提交改动保持 308 行新增、55 行删除，没有混入本轮提交。无关的未跟踪原始证据也保留原样。各检查点仅暂存明确列出的文件。

## 部署准备

部署前只读检查确认 ser8 仍运行 `a76ed7c` 对应版本，容器 healthy。SQLite 在线备份保存到 `/data/backups/pre-known-gaps-785fc2e-20260905.db`，备份完整性检查为 `ok`。备份时为 10 个账户、202 笔交易。部署结果将另行记录，备份完成不代表部署完成。

中文交付检查沿用「好好说话」：保留技术术语和证据限制；新方案机械扫描无异常中文半角标点、空泛评价或模板化标题命中。
