# 协调者独立浏览器复核

服务为 `http://localhost:3101`，使用临时数据库。最初检查的运行版本为 `ca08a02`。没有向正式服务写入测试数据。

## 移动导航通过

390 × 844 视口打开菜单，等待抽屉动画结束后，侧栏横坐标为 0，内部没有币种选择器。点击 Accounts，等待动画结束后，侧栏横坐标为 -288，右边界为 0，URL 为 `#/accounts`。覆盖 `MC-UI-GLOB-007`。

## 净资产默认值与链接互不覆盖

在隔离数据库中将家庭默认列报货币从 CNY 改为 USD，并通过 bootstrap 回读确认。全新文档打开无参数净资产入口，URL 自动补为 `#/reports/net-worth?currency=USD&asOf=2026-09-05`。随后打开显式 `currency=SGD&asOf=2026-09-04` 链接，SGD 按钮的 `aria-pressed` 为 true，日期保持不变。

检查结束后在 finally 中将家庭默认恢复为 CNY。覆盖 `MC-UI-NW-008`。最初用同一文档的 hash 导航时仍持有旧 bootstrap，该次操作没有作为默认值更新的通过证据。

## 账户详情查询参数发现回归

真实打开 `#/accounts/20d3159e-679b-466b-aedc-787681e47700?x=1`，主区域错误显示 Accounts 列表、All Accounts 和 Add Account，没有进入该账户详情。确认 `MC-UI-LINK-008` 失败后派回实施者。

修复提交 `5ffc990` 使用解析后的 path 匹配账户 ID，并保留原 ID 字符范围。新增路由测试与既有 hash 测试共 21 条通过；浏览器复验留给独立验收者在新构建上执行。

## 多币种金额与放大边界

真实收支页同时显示 CNY、USD、SGD 和 JPY 卡片。USD 收入为 0、支出为 37；JPY 支出为 1,234。净资产明细中账户币种、原币余额、换算金额、汇率日期与来源分别列出。例如美元卡原币 -37 USD 对应 -266.40 CNY，人工汇率为 7.20。

尝试通过 Meta+= 触发浏览器放大后，devicePixelRatio、innerWidth 和 visualViewport.scale 均未变化，故本次不声称验证原生浏览器 200% 缩放。独立验收者的 CSS 200% 放大证据仍应注明其方法。

## 工具与文字检查

中文选择器与当前英文界面不匹配、将账户名称误当作 heading 的等待失败均已排除，不算产品缺陷。抽屉几何采用动画结束后的数值，没有使用过渡帧数值。

保留金额、URL、版本和通过边界。中文标点及空泛表述机械扫描无命中；没有将部分验证改写为完成承诺。
